import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    body: {
        color: '#000'
    },
    form: {
        marginTop: 50,
        flex: 1,
    },
});

export default styles;
